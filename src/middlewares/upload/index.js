const uploadProfileImage = require('./profile-image.middleware');
const uploadProducerPhotos = require('./producer-photos.middleware');

module.exports = {
  uploadProfileImage,
  uploadProducerPhotos
};